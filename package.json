{
  "name": "real-time-fair-queue-system",
  "version": "1.0.0",
  "description": "Real-Time Fair Queue System - Smarter Queues. Real-Time Decisions. Less Waiting.",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"cyan,magenta\" \"npm run dev:server\" \"npm run dev:client\"",
    "build": "npm --prefix client install && npm --prefix client run build",
    "build:all": "npm --prefix server install && npm --prefix server run build && npm --prefix client install && npm --prefix client run build",
    "build:server": "npm --prefix server install && npm --prefix server run build",
    "build:client": "npm --prefix client install && npm --prefix client run build",
    "start": "node server/dist/index.js",
    "start:server": "cd server && npm start",
    "test": "cd server && npm test",
    "seed": "cd server && npm run seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
